export default class DropList{
    constructor(_scene){
        this.scene = _scene;
        this.entries = [];
    }

    addEntries(_entries){
        for(let n of _entries){
            
        }
    }
}